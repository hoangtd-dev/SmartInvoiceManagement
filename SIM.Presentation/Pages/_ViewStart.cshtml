@{
    // Choose layout based on the current user identity. If you need a different layout
    // for authenticated users, keep a separate _LayoutAuth file and the check below.
    var isAuthenticated = false;
    Layout = isAuthenticated ? "_LayoutAuth" : "_Layout";
}
